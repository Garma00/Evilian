contact listener per ogni screen (quello per il livello generalizzato)
classe manager di screen con index statico e funzioni change screen con switch

fixare bux quando si preme esc nella schermata di gioco 

tiled
crea object layer per fare i poligoni

classe music manager
implementare una classe music manager che contiene all'interno le varie musiche, vengono tutte istanziate e tramite un intero 
si mette in riproduzione la traccia desiderata

gdx texture packer
